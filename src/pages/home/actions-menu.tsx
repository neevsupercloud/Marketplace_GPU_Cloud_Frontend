// import {VM_T} from "../../types";
// import {useStore} from "../../store";
// import useApi from "../../store/useApi.ts";
// import {Tooltip} from "@mui/material";
// import {IconButton} from "@chakra-ui/react";
// import {BiPause, BiPlay} from "react-icons/bi";
// import {AiFillDelete} from "react-icons/ai";

export default function ActionsMenu() {
  // const {replaceVm} = useStore();
  // const {startVm, stopVm} = useApi();

  // function _startVm() {
  //   startVm(vm.slug).then(vm => vm && replaceVm(vm));
  // }

  // function _stopVm() {
  //   stopVm(vm.slug).then(vm => vm && replaceVm(vm));
  // }

  // function _deleteVm() {
  //   deleteVm();
  // }

  return (
    // <div className="flex items-center">
    //   {vm.actions.start && (
    //     <Tooltip title="Start VM">
    //       <IconButton onClick={_startVm} className="text-2xl text-green-500" aria-label="start-vm">
    //         <BiPlay/>
    //       </IconButton>
    //     </Tooltip>
    //   )}
    //   {vm.actions.stop && (
    //     <Tooltip title="Stop VM">
    //       <IconButton onClick={_stopVm} className="text-2xl text-red-500" aria-label="start-vm">
    //         <BiPause/>
    //       </IconButton>
    //     </Tooltip>
    //   )}
    //   {vm.actions.terminate && (
    //     <Tooltip title="Delete VM">
    //       <IconButton onClick={_deleteVm} className="text-2xl text-red-500" aria-label="start-vm">
    //         <AiFillDelete/>
    //       </IconButton>
    //     </Tooltip>
    //   )}
    // </div>
    <div></div>
  );
}